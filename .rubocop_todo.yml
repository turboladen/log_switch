# No outstanding RuboCop debt: every previously-quarantined offense in lib/ has
# been resolved. This file is intentionally kept (empty) as the inherited parent
# referenced by .rubocop.yml, whose defensive-default pins override any style
# inversion a regeneration would write here.
#
# To regenerate:
#   bundle exec rubocop --auto-gen-config --auto-gen-only-exclude --no-exclude-limit
#
# If a regeneration reintroduces an `EnforcedStyle:` entry here, convert it to an
# `Exclude:` by hand -- see .rubocop.yml for why.
